# Machine-local skill sources — cloned/symlinked per-machine, not committed
google3/
future-of-git/

# Generated by sync.sh — committed for sharing/discovery, but gitignored
# if you prefer not to commit it (e.g. it contains local paths)
# ai-catalog.json
